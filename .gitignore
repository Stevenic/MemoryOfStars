# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Windows
Thumbs.db
Desktop.ini

# Editors
.vscode/
.idea/
*.swp
*~

# Jekyll (website build output & caches)
docs/_site/
docs/.jekyll-cache/
docs/.jekyll-metadata
docs/.sass-cache/
docs/vendor/
docs/.bundle/

# Ruby / Bundler
.bundle/
vendor/

# Manuscript tooling scratch
*.tmp
*.bak
~$*.docx

# Secrets
.env

# planning/ is COMMITTED as of 2026-08 (author decision, contributor onboarding).
# The repo is public, so the roadmap/meta-mystery is world-readable — planning/README.md
# carries the spoiler warning. Personal PDFs dropped in planning/ stay local:
/planning/*.pdf

# The Archive star map's PRIVATE roadmap overlay — full 30-book titles/loglines for
# local UX work only. Production (GitHub Pages) never has this file, so the public
# map keeps its dark, unnamed stars. NEVER COMMIT.
/docs/_data/archive_index_full.yml

# Body-art generator scratch: unpicked variants + the billing log. The PICKED
# finals live in /docs/assets/art/ and ARE committed (public site assets).
/scripts/art/work/
/scripts/art/gen-log.jsonl
