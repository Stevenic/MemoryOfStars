# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Windows
Thumbs.db
Desktop.ini

# Editors
.vscode/
.idea/
*.swp
*~

# Jekyll (website build output & caches)
docs/_site/
docs/.jekyll-cache/
docs/.jekyll-metadata
docs/.sass-cache/
docs/vendor/
docs/.bundle/

# Ruby / Bundler
.bundle/
vendor/

# Manuscript tooling scratch
*.tmp
*.bak
~$*.docx

# Secrets
.env

# Private planning & drafts — NEVER COMMIT.
# The repo is public; spoiler-heavy roadmap/meta-mystery/unwritten-book plans live here
# and must not leak. Anchored to repo root.
/planning/

# The Archive star map's PRIVATE roadmap overlay — full 30-book titles/loglines for
# local UX work only. Production (GitHub Pages) never has this file, so the public
# map keeps its dark, unnamed stars. NEVER COMMIT.
/docs/_data/archive_index_full.yml
